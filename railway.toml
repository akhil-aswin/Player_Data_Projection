[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'uvicorn api:app --host 0.0.0.0 --port $PORT --log-level warning'"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
